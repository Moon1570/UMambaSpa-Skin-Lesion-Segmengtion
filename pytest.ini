[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --strict-markers
    --strict-config
    --ignore=tests/helpers
    -v
markers =
    slow: marks tests as slow
    gpu: marks tests that require GPU
    requires_data: marks tests that require dataset files