_target_: src.data.isic_datamodule.ISICDataModule

data_dir: data/PH2
train_dir: null
val_dir: null
test_dir: data/PH2/test

spatial_mode: "rgb_xyz_radial"
normalize_spatial: true

image_size: [256, 256]
in_channels: 6

batch_size: 8
num_workers: 4
pin_memory: true

augmentation:
  train:
    enabled: false
  val:
    enabled: false
  test:
    enabled: false