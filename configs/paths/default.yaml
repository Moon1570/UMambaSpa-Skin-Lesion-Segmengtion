# Paths configuration - keep it simple, no circular references

# Work directory is current directory
work_dir: .

# Data directory (relative to work_dir)
data_dir: data

# Logs directory (relative to work_dir)
log_dir: logs

# Output directory (set by hydra, don't interpolate)
output_dir: ${hydra:runtime.output_dir}