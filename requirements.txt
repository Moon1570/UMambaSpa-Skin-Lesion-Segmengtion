# Core dependencies (always install)
torch>=2.0.0
torchvision>=0.15.0
pytorch-lightning>=2.0.0
lightning[extra]

# Hydra for configs
hydra-core==1.3.2
hydra-colorlog==1.2.0
hydra-optuna-sweeper==1.2.0

# Data & Augmentation
albumentations>=1.3.0
opencv-python>=4.8.0
Pillow>=9.5.0

# Logging
wandb>=0.15.0
tensorboard>=2.14.0

# --------- others --------- #
rootutils       # standardizing the project root setup
pre-commit      # hooks for applying linters on commit
rich            # beautiful text formatting in terminal
pytest          # tests
# sh            # for running bash commands in some tests (linux/macos only)

# Scientific
numpy>=1.24.0
matplotlib>=3.7.0

# Hugging Face
huggingface-hub>=0.16.0


# OPTIONAL: Mamba (only for Lightning.ai/GPU servers)
# Uncomment these when running on Lightning.ai:
# mamba-ssm>=1.0.0
# causal-conv1d>=1.1.0